# 会话清理与恢复的安全边界

## 删除前

- 完全退出对应 Agent，包括桌面窗口、后台服务和终端中的 Agent 任务。管理器检查已知进程名及 Node / Bun 启动参数；检查失败或发现运行中的 Agent 时拒绝清理，不会自动结束任何进程。
- 手动备份重要的会话数据。会话文件和数据库记录的删除是永久操作，不进入回收站。
- 会话记录不等于项目文件。管理器已移除“同时删除项目目录”选项，后端也拒绝此类请求。需要清理项目文件时，请在文件管理器中单独确认和处理。

进程检查是保护措施，不是与原 Agent 的互斥协议。无法保证识别重命名的进程、自定义运行器，也无法阻止检查后又启动的 Agent。清理期间请勿重新启动对应 Agent，或使用多个管理器实例同时清理。

## 清理范围与失败重试

- 删除当前会话时，也会清理扫描到的同一 Agent、同一原生会话 ID 的历史副本。单独删除历史副本时，仅处理该副本；不会删除其他会话或整个项目。
- Codex 清理参考包含归档记录的索引，并优先清理关联索引，再删除会话文件；归档会话不再仅因未出现在活跃列表中就被当作垃圾。历史副本不会清除仍指向另一份文件的有效索引。
- 路径派生操作校验存储根目录、会话 / 消息 ID 和符号链接边界；ZCode 清理限于所选会话，不按项目级联删除其他任务。
- 开始修改前保存清理清单。索引、附属文件或清理记录未能处理完毕时，返回“待清理”，不显示为完整删除成功。即使原始会话记录已经消失，管理器仍保留可重试的入口。
- 重试使用原始清理清单，不把后来出现的同 ID 副本追加到已有操作中。先解决提示中的锁定、权限或数据格式问题，再点击“继续清理”。

Windows 的清理记录位于 `%LOCALAPPDATA%\AgentSessionManager\pending-deletions`；其他平台位于系统本地应用数据目录下的同名子目录。它只保存定位信息和错误，不是会话内容备份；请勿手动删除未完成的清理记录。记录损坏时管理器拒绝继续删除，避免失去重试依据。

原 Agent 的多个数据库、JSON 索引和文件无法形成一个跨存储事务。待清理记录用于在部分失败后继续收尾，不提供撤销或内容恢复。原 Agent 数据格式升级后仍可能需要更新适配器；如果原 Agent 重启后仍有侧边栏残留，请保留错误信息和清理记录，避免手动批量删库。

## 查看与恢复

- 列表区分归档、历史副本和待清理状态。归档 / 副本 / 待清理记录不提供直接恢复，请先在原 Agent 中处理。
- 恢复命令由后端根据选中的会话重新生成、校验 ID 并转义工作路径，不执行会话历史中保存的任意命令。Codex 使用会话本身的工作目录，而不是侧边栏项目分组目录。
- Windows 复制的是 PowerShell 命令，Linux 复制的是 shell 命令；macOS 可打开终端。需要本机已经安装对应 Agent，且工作目录仍存在。
- 列表分页每页 100 条；“全选筛选结果”包含其他分页。重新扫描也会刷新消息缓存。扫描警告不代表没有会话，读取失败也不等于空会话。
- SQLite 单次消息读取限制为每表 20,000 条 / 32 MiB，OpenCode 旧版 JSON 会话合计读取限制为 32 MiB，整文件消息读取限制为 128 MiB，扫描时单行元数据限制为 4 MiB。超限会提示，不应通过删除会话解决；请用原 Agent 查看。
